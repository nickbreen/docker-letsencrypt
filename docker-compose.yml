version: '2'
services:
  le:
    image: nickbreen/letsencrypt:v1.2.0
    build:
        context: .
    volumes:
      - /certs # mount this from the haproxy
      - /var/www # mount this from the web server
    environment:
      CRON_D_LE: |
        @monthly root le renew | logger
